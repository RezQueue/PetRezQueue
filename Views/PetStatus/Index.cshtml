@model IEnumerable<RezzQueue.Models.PetStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AnimalId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CustomerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HasSeen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Favorite)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ThumbsDown)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AnimalId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CustomerId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HasSeen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Favorite)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ThumbsDown)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PetStatusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PetStatusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PetStatusId })
        </td>
    </tr>
}

</table>
