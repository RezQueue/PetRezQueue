@model IEnumerable<RezzQueue.Models.Breed>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Species.SpeciesName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BreedName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Species.SpeciesName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BreedName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BreedId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BreedId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BreedId })
        </td>
    </tr>
}

</table>
